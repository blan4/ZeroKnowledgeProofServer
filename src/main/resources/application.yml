domainName: '192.168.0.102:9000'
redisURL: redis://localhost:6879

server:
  port: 9000


spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/qrauth_dev
    username: qrauth
    password: qrauthpwd

  redis:
    host: localhost
    port: 6379
    password:

  devtools:
    restart:
      enabled: true

  jade4j:
    caching: false
    prettyPrint: true

flyway:
  baseline-on-migrate: true
  clean-on-validation-error: true

